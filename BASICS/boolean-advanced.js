let isAccountLocked = false

//here if the account is locked it will type is account locked
//if it is false - it will type welcome
if (isAccountLocked) {
    console.log("Is Account Locked")
} else {
    console.log('Welcome!')
}